# AMTECH Article Publishing Agent

This workflow now lives as a repo-local Codex skill:

```text
docs/skills/amtech-article-publisher/SKILL.md
```

Use `docs/skills/amtech-article-publisher/` when an agent receives article copy in pasted text, markdown, HTML, XML, JSON, a draft outline, or edited article material and needs to publish it into AMTECH's live article system, knowledge graph, OKF bundle, prerendered route, and optional Supabase projection.

This file remains as a compatibility pointer for older documentation and agent memory that referenced `docs/ARTICLE_PUBLISHING_AGENT.md`.
