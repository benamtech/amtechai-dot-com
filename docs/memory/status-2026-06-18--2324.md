# AMTECH Status Update - 6/18 23:24

Purpose: most recent handoff after converting article workflows into skills and publishing the OKF explainer into the live article/OKF system.

## What Changed

### Repo-local article skills

- Added `docs/skills/amtech-article-publisher/` as a proper Codex skill.
- Kept `docs/skills/amtech-article-research-writer/` as the draft/research skill.
- Converted `docs/ARTICLE_PUBLISHING_AGENT.md` into a compatibility pointer to the publisher skill.
- Updated article workflow references in `docs/codegraph.md`, `codegraph.json`, `docs/okf/README.md`, `docs/memory/README.md`, `docs/memory/fast-navigation-and-doc-sync.md`, and the article research skill.

### Published article

- Published draft `docs/article-drafts/okf-ai-readable-knowledge.md` to:
  - Route: `/articles/what-is-okf-ai-readable-knowledge`
  - Article data: `src/lib/knowledge/articles/what-is-okf-ai-readable-knowledge.ts`
  - Wrapper page: `src/pages/articles/OkfAiReadableKnowledge.tsx`
  - Graph node: `E9`
  - Topic group: `Knowledge graph and agentic search`
- Added the article to the article hub, `src/lib/knowledge/articles/index.ts`, `src/App.tsx`, and `src/lib/articleKnowledgeGraph.ts`.
- Regenerated `public/okf/**`, `public/sitemap.xml`, `public/robots.txt`, and `public/llms.txt`.
- Updated article-system docs to 9 published article routes and 74 OKF concepts.

## Key Insights

- The clean split is now:
  - `amtech-article-research-writer`: create research packets and markdown drafts under `docs/article-drafts/`.
  - `amtech-article-publisher`: publish approved/supplied copy into the live article graph, route, OKF bundle, prerendered page, and optional Supabase projection.
- OKF should be treated as a generated projection, not the canonical source. The canonical source remains `src/lib/knowledge/`, with article data, graph nodes, entities, citations, and edges projecting into React pages, prerendered HTML, OKF markdown, discovery files, and seed SQL.
- Example markdown links inside article prompt/code blocks can be interpreted by the OKF validator as bundle links. For published OKF articles, avoid fake markdown links in examples unless the target exists in `public/okf/**`.
- The Supabase knowledge tables are still a projection. Publishing a new article updates the source and generated static surfaces, but the live DB projection must be refreshed separately through `npm run okf:db:seed-sql` and a service-role-safe apply path.

## Validation Run

- `quick_validate.py docs/skills/amtech-article-publisher` passed.
- `quick_validate.py docs/skills/amtech-article-research-writer` passed.
- `npm run typecheck` passed.
- `npm run okf:check` passed: 74 concepts, 0 warnings, 0 errors.
- `npm run build` passed and prerendered 11 static routes: 9 article routes plus `/articles` and `/articles/all`.
- `npm run lint` passed with 2 existing warnings in `src/components/sales-rep-apply/salesRepShared.tsx`.
- `npm run okf:validate:phase3` passed with the expected manual G7 warning.
- `git diff --check` passed.

## Supabase Projection Status

- Generated compact seed SQL locally at `/tmp/okf-seed.sql` with `node scripts/okf/db-seed-sql.ts --no-body`.
- Did not apply the seed SQL to Supabase. Apply it through a service-role-safe path and then run `npm run okf:db:verify` if the live DB projection must include article `E9`.

## Next Agent Starting Points

1. Start article research with `docs/skills/amtech-article-research-writer/SKILL.md`.
2. Start article publishing with `docs/skills/amtech-article-publisher/SKILL.md`.
3. For OKF/article graph work, check `docs/codegraph.md`, `docs/okf/README.md`, `docs/okf/04-validation-and-phase-gates.md`, and this handoff before broad searches.
4. If touching `src/lib/knowledge/articles/*`, `src/lib/articleKnowledgeGraph.ts`, or `src/lib/knowledge/entities.ts`, run `npm run okf:check` and commit regenerated `public/okf/**`, `public/sitemap.xml`, `public/robots.txt`, and `public/llms.txt`.
